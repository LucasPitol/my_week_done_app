/// Font family names used across the app.
abstract final class AppFonts {
  /// Display and heading type — bundled from Fontshare (variable font).
  static const String satoshi = 'Satoshi';

  /// Body and UI copy — loaded via [google_fonts] package.
  static const String inter = 'Inter';
}
